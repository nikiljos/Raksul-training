// import Member from "../models/member.model";

// const addMember = async (groupData: {
//   groupName: string | null;
//   admin: number;
// }) => {
//   const { groupName, admin } = groupData;
//   console.log(groupData);
//   try {
//     const group = await Member.create({ name: groupName, admin });
//   } catch (error) {
//     console.log("error, ", error);
//     throw new Error("Error creating group");
//   }
// };

// export default {
//   addMember,
// };
